# ── INPUT (Gate-1, you edit) — SHAs to force-pick as PICK-RESOLVE (3-way-merged at execute). One per line; '#' = comment. See ../README.md ──
# force-PICK (PICK-RESOLVE) genuine Spectro fork cloud-service behavior that MUST carry forward.
# Reviewer-applied at Gate-1 (PCP-6871 / v2.11.1). Each is a Spectro production fix (PCP/BET ticket),
# absent from target history (git log v2.11.1 --grep "PCP-" -> none), isolated to pkg/cloud|exp|bootstrap
# code (NOT generated, NOT MIXED, NOT the webhook/main.go split). Conflict = target's surrounding code
# moved; resolver 3-way merges at execute, human reviews the resolved diff at Gate 2. Risk M in
# regwatch-risk.tsv (does not block; Gate-2 reviewed). NOT included: the OIDC/IAM security stack and
# the webhook/controller-separation main.go commits (left NEEDS-DECISION for the human — see report).

0b29ddfe  # PCP-1428 consume AWS FIPS endpoints (pkg/cloud/scope/session.go) — fork-only FIPS session support
7226c861  # PCP-259 EKS cluster post-upgrade fix (pkg/cloud/services/eks/config.go)
4b21d29a  # log issues when node group degraded (eks/config.go, eks/nodegroup.go)
96eb647e  # volumetags-hotfix: attach tags to volume (pkg/cloud/services/ec2/instances.go)
2c331b16  # PCP-3571 EKS SG tag updation from AWSManagedControlPlane (eks/securitygroup.go)
f108d78c  # return correct errors (eks/config.go)
debdb73d  # fix duplicate tag InvalidParameterValue (ec2/instances.go)
c3dfe72b  # PCP-4653 node pool customization without AMI ID (eks/nodegroup.go)
52817669  # hotfix launchtemplate id not found (exp/controllers/awsmanagedmachinepool_controller.go)
1a891780  # handle nil pointer launchTemplateID (exp/controllers/awsmanagedmachinepool_controller.go)
c9ad0d74  # PCP-4952 EKS AL2023 custom AMI dynamic creds/STS (bootstrap/eks/controllers/eksconfig_controller.go)
a2477966  # PCP-5325 node customization on AWS secret region (bootstrap/eks/controllers/eksconfig_controller.go)
b9f3f86a  # PCP-5517 set clustercidr and dns in NodeInput (bootstrap/eks/controllers/eksconfig_controller.go)
f73e9bb3  # eks: skip aws:-prefixed tags in nodegroup reconciliation (eks/tags.go)
e6d443e2  # do not set amiType if EKSOptimizedLookupType is set (eks/nodegroup.go)
5ba61538  # PCP-6865 bypass nodegroup version API call (eks/nodegroup.go)
4d05d66e  # fix addons comparison (pkg/eks/addons/types.go)
